# CCF secrets — copy this file to secrets.env and fill in only the keys you use.
#
#   cp secrets.env.example secrets.env
#   chmod 600 secrets.env
#
# NEVER commit secrets.env (it is gitignored). Each value is the API key/token for
# a provider declared in providers.json via its "key_env" field.

# z.ai — GLM-5.2, 1M context, zero-retention. https://z.ai/manage-apikey/apikey-list
ZAI_API_KEY=

# OpenCode Go — flat-rate subscription (DeepSeek-V4-Pro 1M, etc), zero-retention. https://opencode.ai
OPENCODE_GO_KEY=

# OpenCode Zen — pay-per-token. NOT zero-retention; do not send proprietary code. https://opencode.ai
OPENCODE_ZEN_KEY=

# OpenAI Codex subscription (GPT-5.5 panelist).
# Easiest: browser login (no codex CLI needed):   ~/.claude/fusion/ccf-codex-login
#   -> writes ~/.codex/auth.json; fusion-call reads + auto-refreshes it. Then /fusion-config enable gpt.
# Already use the codex CLI? 'codex auth login' works too (same file).
# Manual override (rarely needed):
# CODEX_ACCESS_TOKEN=
# CODEX_ACCOUNT_ID=

# Tavily — web search for panelists (optional, enables --search flag)
# Get key at: https://tavily.com
# TAVILY_API_KEY=
# MAX_SEARCH_TURNS=3

# Optional — only if you add these providers via /fusion-config:
# GEMINI_API_KEY=
# MOONSHOT_API_KEY=
# OPENROUTER_API_KEY=
